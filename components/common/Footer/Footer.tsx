import { FunctionComponent } from 'react';
import * as React from 'react';
import styles from './footer.module.scss';

export const Footer: FunctionComponent = () => <div className={styles.footer} />;
